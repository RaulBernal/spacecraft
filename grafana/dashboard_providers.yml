apiVersion: 1

providers:
  - name: 'default'
    orgId: 1
    folder: ''  # default maped folder for dashboards
    type: file
    disableDeletion: false
    editable: true
    options:
      #path: /var/lib/grafana/dashboards
      /opt/gno/data
